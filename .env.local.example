# Required
ETHERSCAN_API_KEY=your_etherscan_api_key

# Optional — defaults to public RPCs if unset
ETH_RPC_URL=
BASE_RPC_URL=

# Required for prose generation (structured output works without it)
ANTHROPIC_API_KEY=your_anthropic_api_key
